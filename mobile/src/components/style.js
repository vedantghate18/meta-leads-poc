import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  card: {
    padding: 16,
    borderRadius: 12,
    borderWidth: 1,
    marginBottom: 12,
  },

  name: {
    fontSize: 18,
    fontWeight: '600',
    marginBottom: 6,
  },

  email: {
    marginBottom: 4,
  },

  phone: {
    color: '#666',
  },
});

export default style;
